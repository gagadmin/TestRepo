<script setup>
/**
 * Unauthenticated shell.
 *
 * Deliberately a bare passthrough: the sign-in page renders the full-bleed
 * two-panel `auth-shell` layout itself, so any wrapper here would fight its
 * own grid and centring.
 */
</script>

<template>
    <RouterView />
</template>
